[
  ".relative",
  "/bills?page=${pageParam}&limit=15&search=${encodeURIComponent(debouncedSearch)}&status=${encodeURIComponent(filterStatus)}",
  "/customers?limit=1000",
  "/products?limit=1000",
  "© ${new Date().getFullYear()} UdharPay. All rights reserved.",
  "/customers?page=${pageParam}&limit=15&search=${encodeURIComponent(debouncedSearch)}&filterBalance=${encodeURIComponent(filterBalance)}",
  "Are you sure you want to delete ${customer.name}? This action cannot be undone.",
  "/reports/dashboard",
  "/notifications?page=${pageParam}&limit=15",
  "/products?page=${pageParam}&limit=15&search=${encodeURIComponent(debouncedSearch)}&filterStock=${encodeURIComponent(filterStock)}",
  "${t('Generated on",
  "/reminders?page=${pageParam}&limit=15&search=${encodeURIComponent(debouncedSearch)}&filterStatus=${encodeURIComponent(filterStatus)}",
  "/reminders/${reminderId}/send",
  "Are you sure you want to delete this reminder? This action cannot be undone.",
  "You have ${stats.customersWithDuesCount} customer(s) with pending dues. Consider sending a reminder to ${stats.topDefaulter.name} who owes ₹${stats.topDefaulter.balance.toLocaleString()} to improve your cash flow.",
  "/bills",
  "/transactions",
  "${t('Period",
  "${t('Total Bills",
  "${t('Total Sales",
  "${t('Total Collections",
  "/settings",
  "@",
  "We've sent a 6-digit verification code to",
  "/transactions/customer/${customer._id}",
  "/notifications/unread-count",
  "/notifications?limit=5",
  "/auth/logout"
]